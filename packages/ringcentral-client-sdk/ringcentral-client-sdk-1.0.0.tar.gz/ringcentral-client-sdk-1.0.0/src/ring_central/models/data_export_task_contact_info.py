# This file was generated by liblab | https://liblab.com/

from .utils.json_map import JsonMap
from .base import BaseModel


@JsonMap({"id_": "id"})
class DataExportTaskContactInfo(BaseModel):
    """List of users whose data is collected. The following data will be exported: posts, tasks, events, etc. posted by the user(s); posts addressing the user(s) via direct and @Mentions; tasks assigned to the listed user(s). The list of 10 users per request is supported.

    :param id_: Internal identifier of a contact, defaults to None
    :type id_: str, optional
    :param email: Email address of a contact, defaults to None
    :type email: str, optional
    """

    def __init__(self, id_: str = None, email: str = None):
        if id_ is not None:
            self.id_ = id_
        if email is not None:
            self.email = email
