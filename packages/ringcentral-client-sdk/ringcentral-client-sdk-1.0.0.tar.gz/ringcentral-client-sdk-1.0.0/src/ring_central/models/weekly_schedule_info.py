# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from typing import List
from .utils.json_map import JsonMap
from .base import BaseModel
from .time_interval import TimeInterval


@JsonMap({})
class WeeklyScheduleInfo(BaseModel):
    """Weekly schedule

    :param monday: Time intervals for a particular day, defaults to None
    :type monday: List[TimeInterval], optional
    :param tuesday: Time intervals for a particular day, defaults to None
    :type tuesday: List[TimeInterval], optional
    :param wednesday: Time intervals for a particular day, defaults to None
    :type wednesday: List[TimeInterval], optional
    :param thursday: Time intervals for a particular day, defaults to None
    :type thursday: List[TimeInterval], optional
    :param friday: Time intervals for a particular day, defaults to None
    :type friday: List[TimeInterval], optional
    :param saturday: Time intervals for a particular day, defaults to None
    :type saturday: List[TimeInterval], optional
    :param sunday: Time intervals for a particular day, defaults to None
    :type sunday: List[TimeInterval], optional
    """

    def __init__(
        self,
        monday: List[TimeInterval] = None,
        tuesday: List[TimeInterval] = None,
        wednesday: List[TimeInterval] = None,
        thursday: List[TimeInterval] = None,
        friday: List[TimeInterval] = None,
        saturday: List[TimeInterval] = None,
        sunday: List[TimeInterval] = None,
    ):
        if monday is not None:
            self.monday = self._define_list(monday, TimeInterval)
        if tuesday is not None:
            self.tuesday = self._define_list(tuesday, TimeInterval)
        if wednesday is not None:
            self.wednesday = self._define_list(wednesday, TimeInterval)
        if thursday is not None:
            self.thursday = self._define_list(thursday, TimeInterval)
        if friday is not None:
            self.friday = self._define_list(friday, TimeInterval)
        if saturday is not None:
            self.saturday = self._define_list(saturday, TimeInterval)
        if sunday is not None:
            self.sunday = self._define_list(sunday, TimeInterval)
