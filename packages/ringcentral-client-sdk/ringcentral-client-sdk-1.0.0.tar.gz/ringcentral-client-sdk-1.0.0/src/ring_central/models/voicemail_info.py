# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from .utils.json_map import JsonMap
from .base import BaseModel
from .recipient_info import RecipientInfo


@JsonMap({})
class VoicemailInfo(BaseModel):
    """Specifies whether to take a voicemail and who should do it

    :param enabled: If `true` then voicemails are allowed to be received, defaults to None
    :type enabled: bool, optional
    :param recipient: Recipient data, defaults to None
    :type recipient: RecipientInfo, optional
    """

    def __init__(self, enabled: bool = None, recipient: RecipientInfo = None):
        if enabled is not None:
            self.enabled = enabled
        if recipient is not None:
            self.recipient = self._define_object(recipient, RecipientInfo)
