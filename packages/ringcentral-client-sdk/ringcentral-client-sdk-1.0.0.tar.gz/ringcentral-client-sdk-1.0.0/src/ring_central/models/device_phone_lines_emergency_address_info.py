# This file was generated by liblab | https://liblab.com/

from .utils.json_map import JsonMap
from .base import BaseModel


@JsonMap({"local_only": "localOnly"})
class DevicePhoneLinesEmergencyAddressInfo(BaseModel):
    """DevicePhoneLinesEmergencyAddressInfo

    :param required: Indicates that emergency address is required, defaults to None
    :type required: bool, optional
    :param local_only: Indicates that only local emergency address can be specified, defaults to None
    :type local_only: bool, optional
    """

    def __init__(self, required: bool = None, local_only: bool = None):
        if required is not None:
            self.required = required
        if local_only is not None:
            self.local_only = local_only
