# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from typing import List
from .utils.json_map import JsonMap
from .base import BaseModel
from .location_info import LocationInfo
from .page_navigation_model import PageNavigationModel
from .enumerated_paging_model import EnumeratedPagingModel


@JsonMap({})
class GetLocationListResponse(BaseModel):
    """GetLocationListResponse

    :param uri: Link to the location list resource, defaults to None
    :type uri: str, optional
    :param records: List of locations, defaults to None
    :type records: List[LocationInfo], optional
    :param navigation: Links to other pages of the current result set
    :type navigation: PageNavigationModel
    :param paging: paging
    :type paging: EnumeratedPagingModel
    """

    def __init__(
        self,
        navigation: PageNavigationModel,
        paging: EnumeratedPagingModel,
        uri: str = None,
        records: List[LocationInfo] = None,
    ):
        if uri is not None:
            self.uri = uri
        if records is not None:
            self.records = self._define_list(records, LocationInfo)
        self.navigation = self._define_object(navigation, PageNavigationModel)
        self.paging = self._define_object(paging, EnumeratedPagingModel)
