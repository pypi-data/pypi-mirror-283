# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from typing import List
from .utils.json_map import JsonMap
from .base import BaseModel
from .site_ivr_top_menu import SiteIvrTopMenu
from .site_ivr_actions import SiteIvrActions


@JsonMap({"top_menu": "topMenu"})
class SiteIvrSettings(BaseModel):
    """SiteIvrSettings

    :param top_menu: Top IVR Menu extension. Mandatory for MultiLevel mode<br>, defaults to None
    :type top_menu: SiteIvrTopMenu, optional
    :param actions: actions, defaults to None
    :type actions: List[SiteIvrActions], optional
    """

    def __init__(
        self, top_menu: SiteIvrTopMenu = None, actions: List[SiteIvrActions] = None
    ):
        if top_menu is not None:
            self.top_menu = self._define_object(top_menu, SiteIvrTopMenu)
        if actions is not None:
            self.actions = self._define_list(actions, SiteIvrActions)
