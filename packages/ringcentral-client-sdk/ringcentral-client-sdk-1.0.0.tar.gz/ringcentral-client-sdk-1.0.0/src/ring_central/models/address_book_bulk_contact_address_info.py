# This file was generated by liblab | https://liblab.com/

from .utils.json_map import JsonMap
from .base import BaseModel


@JsonMap({})
class AddressBookBulkContactAddressInfo(BaseModel):
    """AddressBookBulkContactAddressInfo

    :param country: Country name of the extension user company. Not returned for Address<br>Book<br>, defaults to None
    :type country: str, optional
    :param state: State/province name of the extension user company, defaults to None
    :type state: str, optional
    :param city: City name of the extension user company, defaults to None
    :type city: str, optional
    :param street: Street address of the extension user company, defaults to None
    :type street: str, optional
    :param zip: Zip code of the extension user company, defaults to None
    :type zip: str, optional
    """

    def __init__(
        self,
        country: str = None,
        state: str = None,
        city: str = None,
        street: str = None,
        zip: str = None,
    ):
        if country is not None:
            self.country = country
        if state is not None:
            self.state = state
        if city is not None:
            self.city = city
        if street is not None:
            self.street = street
        if zip is not None:
            self.zip = zip
