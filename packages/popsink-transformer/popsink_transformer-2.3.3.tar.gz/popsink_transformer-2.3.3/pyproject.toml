[tool.poetry]
name = "popsink-transformer"
version = "2.3.3"
description = "Popsink transformer"
authors = ["Popsink <contact@popsink.com>"]
readme = "README.md"
packages = [{include = "popsink_transformer"}]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
