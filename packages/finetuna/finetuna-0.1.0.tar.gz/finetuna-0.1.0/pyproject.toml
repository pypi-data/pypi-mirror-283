[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[project]
name = "finetuna"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "accelerate",
    "diffusers",
    "evaluate",
    "huggingface",
    "pyyaml",
    "scikit-learn",
    "torch",
    "torchsummary",
    "tqdm",
    "transformers",
]

[tool]
