[flake8]
exclude = env*,.tox,.git,*.egg,build

[tool:pytest]
testpaths = tests
addopts = --cov=dserver_search_plugin_mongo

[cov:run]
source = dserver_search_plugin_mongo

[egg_info]
tag_build = 
tag_date = 0

