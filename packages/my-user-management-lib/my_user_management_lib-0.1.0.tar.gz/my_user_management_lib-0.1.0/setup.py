from setuptools import setup, find_packages

setup(
    name='my_user_management_lib',
    version='0.1.0',
    description='User Management Library',
    author='',
    author_email='',
    packages=find_packages(),
    install_requires=[
        'requests'
    ],
)