# This file was generated by liblab | https://liblab.com/

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ups-rating"
version = "1.0.0"
license = { text = "MIT" }
description = """Compare delivery services and shipping rates to determine the best option."""
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.31.0"
]
