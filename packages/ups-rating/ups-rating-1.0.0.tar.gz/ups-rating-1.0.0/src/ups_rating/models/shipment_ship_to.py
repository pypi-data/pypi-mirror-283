# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from .utils.json_map import JsonMap
from .base import BaseModel
from .ship_to_address import ShipToAddress


@JsonMap({"name": "Name", "attention_name": "AttentionName", "address": "Address"})
class ShipmentShipTo(BaseModel):
    """Ship To Container

    :param name: Destination attention name or company name.  Length is not validated., defaults to None
    :type name: str, optional
    :param attention_name: Destination attention name.  Length is not validated., defaults to None
    :type attention_name: str, optional
    :param address: Address Container.
    :type address: ShipToAddress
    """

    def __init__(
        self, address: ShipToAddress, name: str = None, attention_name: str = None
    ):
        if name is not None:
            self.name = name
        if attention_name is not None:
            self.attention_name = attention_name
        self.address = self._define_object(address, ShipToAddress)
