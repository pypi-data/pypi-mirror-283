# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper
from mergedapi.models.compliance_scan import ComplianceScan
from mergedapi.models.paging import Paging


class ComplianceListScansResponse(object):

    """Implementation of the 'complianceListScansResponse' model.

    TODO: type model description here.

    Attributes:
        scans (List[ComplianceScan]): TODO: type description here.
        paging (Paging): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "scans": 'scans',
        "paging": 'paging'
    }

    _optionals = [
        'scans',
        'paging',
    ]

    def __init__(self,
                 scans=APIHelper.SKIP,
                 paging=APIHelper.SKIP):
        """Constructor for the ComplianceListScansResponse class"""

        # Initialize members of the class
        if scans is not APIHelper.SKIP:
            self.scans = scans 
        if paging is not APIHelper.SKIP:
            self.paging = paging 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        scans = None
        if dictionary.get('scans') is not None:
            scans = [ComplianceScan.from_dictionary(x) for x in dictionary.get('scans')]
        else:
            scans = APIHelper.SKIP
        paging = Paging.from_dictionary(dictionary.get('paging')) if 'paging' in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(scans,
                   paging)
