# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper
from mergedapi.models.policy import Policy


class TaggingProfileSchema(object):

    """Implementation of the 'tagging-profile_schema' model.

    TODO: type model description here.

    Attributes:
        description (str): Description about this tagging profile
        name (str): Tagging profile name. Should be of pattern
            [a-zA-Z][a-zA-Z0-9_-]*
        policy (List[Policy]): Policy details

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": 'name',
        "description": 'description',
        "policy": 'policy'
    }

    _optionals = [
        'description',
        'policy',
    ]

    def __init__(self,
                 name=None,
                 description=APIHelper.SKIP,
                 policy=APIHelper.SKIP):
        """Constructor for the TaggingProfileSchema class"""

        # Initialize members of the class
        if description is not APIHelper.SKIP:
            self.description = description 
        self.name = name 
        if policy is not APIHelper.SKIP:
            self.policy = policy 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        name = dictionary.get("name") if dictionary.get("name") else None
        description = dictionary.get("description") if dictionary.get("description") else APIHelper.SKIP
        policy = None
        if dictionary.get('policy') is not None:
            policy = [Policy.from_dictionary(x) for x in dictionary.get('policy')]
        else:
            policy = APIHelper.SKIP
        # Return an object of this model
        return cls(name,
                   description,
                   policy)
