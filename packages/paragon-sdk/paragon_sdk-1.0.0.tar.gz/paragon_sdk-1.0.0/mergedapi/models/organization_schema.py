# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper
from mergedapi.models.site_schema import SiteSchema


class OrganizationSchema(object):

    """Implementation of the 'organization_schema' model.

    TODO: type model description here.

    Attributes:
        description (str): Description about the organization
        organization_name (str): Name of the organization. Should be of
            pattern [a-zA-Z][a-zA-Z0-9_-]*
        site (List[SiteSchema]): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "organization_name": 'organization-name',
        "description": 'description',
        "site": 'site'
    }

    _optionals = [
        'description',
        'site',
    ]

    def __init__(self,
                 organization_name=None,
                 description=APIHelper.SKIP,
                 site=APIHelper.SKIP):
        """Constructor for the OrganizationSchema class"""

        # Initialize members of the class
        if description is not APIHelper.SKIP:
            self.description = description 
        self.organization_name = organization_name 
        if site is not APIHelper.SKIP:
            self.site = site 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        organization_name = dictionary.get("organization-name") if dictionary.get("organization-name") else None
        description = dictionary.get("description") if dictionary.get("description") else APIHelper.SKIP
        site = None
        if dictionary.get('site') is not None:
            site = [SiteSchema.from_dictionary(x) for x in dictionary.get('site')]
        else:
            site = APIHelper.SKIP
        # Return an object of this model
        return cls(organization_name,
                   description,
                   site)
