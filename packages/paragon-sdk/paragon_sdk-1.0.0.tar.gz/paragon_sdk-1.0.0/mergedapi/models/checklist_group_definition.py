# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper


class ChecklistGroupDefinition(object):

    """Implementation of the 'checklistGroupDefinition' model.

    GroupDefinition represents a group, imported from an XCCDF document.

    Attributes:
        rule_set_index (int): Index of the template RuleSet which defined this
            group.
        parent_index (int): Index of the group that contains the group, or -1
            if the group is a top-level group.
        source_id (str): The ID of the group element within the original
            source.
        title (str): The title of the group, from the original source.  TODO
            Add other properties from the XCCDF group definition, as
            required.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "rule_set_index": 'ruleSetIndex',
        "parent_index": 'parentIndex',
        "source_id": 'sourceId',
        "title": 'title'
    }

    _optionals = [
        'rule_set_index',
        'parent_index',
        'source_id',
        'title',
    ]

    def __init__(self,
                 rule_set_index=APIHelper.SKIP,
                 parent_index=APIHelper.SKIP,
                 source_id=APIHelper.SKIP,
                 title=APIHelper.SKIP):
        """Constructor for the ChecklistGroupDefinition class"""

        # Initialize members of the class
        if rule_set_index is not APIHelper.SKIP:
            self.rule_set_index = rule_set_index 
        if parent_index is not APIHelper.SKIP:
            self.parent_index = parent_index 
        if source_id is not APIHelper.SKIP:
            self.source_id = source_id 
        if title is not APIHelper.SKIP:
            self.title = title 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        rule_set_index = dictionary.get("ruleSetIndex") if dictionary.get("ruleSetIndex") else APIHelper.SKIP
        parent_index = dictionary.get("parentIndex") if dictionary.get("parentIndex") else APIHelper.SKIP
        source_id = dictionary.get("sourceId") if dictionary.get("sourceId") else APIHelper.SKIP
        title = dictionary.get("title") if dictionary.get("title") else APIHelper.SKIP
        # Return an object of this model
        return cls(rule_set_index,
                   parent_index,
                   source_id,
                   title)
