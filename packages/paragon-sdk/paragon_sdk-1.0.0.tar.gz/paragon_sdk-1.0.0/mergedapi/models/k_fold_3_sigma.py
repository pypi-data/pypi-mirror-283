# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper
from mergedapi.models.sensitivity import Sensitivity


class KFold3sigma(object):

    """Implementation of the 'KFold3sigma' model.

    TODO: type model description here.

    Attributes:
        learning_period (str): Time period on which to detect outliers
        sensitivity (Sensitivity): Sensitivity to outliers: high sensitivity
            detects more outliers
        sigma_coefficient (float): Number of standard deviations past which
            outliers are marked

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "learning_period": 'learning-period',
        "sensitivity": 'sensitivity',
        "sigma_coefficient": 'sigma-coefficient'
    }

    _optionals = [
        'sensitivity',
        'sigma_coefficient',
    ]

    def __init__(self,
                 learning_period=None,
                 sensitivity=APIHelper.SKIP,
                 sigma_coefficient=APIHelper.SKIP):
        """Constructor for the KFold3sigma class"""

        # Initialize members of the class
        self.learning_period = learning_period 
        if sensitivity is not APIHelper.SKIP:
            self.sensitivity = sensitivity 
        if sigma_coefficient is not APIHelper.SKIP:
            self.sigma_coefficient = sigma_coefficient 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        learning_period = dictionary.get("learning-period") if dictionary.get("learning-period") else None
        sensitivity = Sensitivity.from_dictionary(dictionary.get('sensitivity')) if 'sensitivity' in dictionary.keys() else APIHelper.SKIP
        sigma_coefficient = dictionary.get("sigma-coefficient") if dictionary.get("sigma-coefficient") else APIHelper.SKIP
        # Return an object of this model
        return cls(learning_period,
                   sensitivity,
                   sigma_coefficient)
