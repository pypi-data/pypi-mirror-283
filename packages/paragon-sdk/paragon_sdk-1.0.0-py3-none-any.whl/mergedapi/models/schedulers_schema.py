# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper
from mergedapi.models.scheduler_schema import SchedulerSchema


class SchedulersSchema(object):

    """Implementation of the 'schedulers_schema' model.

    TODO: type model description here.

    Attributes:
        scheduler (List[SchedulerSchema]): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "scheduler": 'scheduler'
    }

    _optionals = [
        'scheduler',
    ]

    def __init__(self,
                 scheduler=APIHelper.SKIP):
        """Constructor for the SchedulersSchema class"""

        # Initialize members of the class
        if scheduler is not APIHelper.SKIP:
            self.scheduler = scheduler 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        scheduler = None
        if dictionary.get('scheduler') is not None:
            scheduler = [SchedulerSchema.from_dictionary(x) for x in dictionary.get('scheduler')]
        else:
            scheduler = APIHelper.SKIP
        # Return an object of this model
        return cls(scheduler)
