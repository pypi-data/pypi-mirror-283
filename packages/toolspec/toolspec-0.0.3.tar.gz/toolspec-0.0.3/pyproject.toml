[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "toolspec"
version = "0.0.3"
authors = [
  { name="Xiaoqing Chen", email="1092640727@qq.com" },
]
description = "This tool is so special that it may not run properly."
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
