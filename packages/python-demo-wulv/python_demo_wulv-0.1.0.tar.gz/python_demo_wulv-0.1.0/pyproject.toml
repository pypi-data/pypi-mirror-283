[tool.poetry]
name = "python-demo-wulv"
version = "0.1.0"
description = ""
authors = ["无弃 <wuqi.wl@alibaba-inc.com>"]
readme = "README.md"
packages = [{include = "python_demo"}]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.32.3"


[tool.poetry.group.dev.dependencies]
pytest = "^8.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
