""" Example of simple math

The module contains:
- `add(a, b)` - Returns the sum of two numbers
"""

def add(a: float, b: float) -> float:
    """ Compute the sum ($\Sigma$) of two numbers

    $$ y = a + b $$

    Args:
        a: First number to add
        b: Second number to add

    Returns:
        Arithmetic sum of `a` and `b`
    """
    return a + b
