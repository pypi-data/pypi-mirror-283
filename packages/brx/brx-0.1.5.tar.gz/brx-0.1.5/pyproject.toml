[tool.poetry]
name = "brx"
version = "0.1.5"
description = "Use the BRX API using Python."
authors = ["BRX"]
readme = "README.md"
packages = [{include = "brx"}]

[tool.poetry.dependencies]
python = "^3.10"
websocket-client = "^1.7.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
