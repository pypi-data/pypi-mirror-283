[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ibm-mq-cli"
version = "0.6.3"
description = "IBM MQ Management CLI Tool"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Brian Hartford", email = "bh419@protonmail.com" }
]
dependencies = []

[project.scripts]
ibm-mq-cli = "ibm_mq_cli.main:main"
