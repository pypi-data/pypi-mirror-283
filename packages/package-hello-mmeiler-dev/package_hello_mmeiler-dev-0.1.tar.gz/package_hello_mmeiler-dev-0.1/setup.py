from setuptools import setup, find_packages

setup(
        name="package_hello_mmeiler-dev",
        version="0.1",
        pacakges=find_packages(),
        install_requires=[
            
        ],
    )
